<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework></TargetFramework>
    <TargetFrameworks>net9.0;net10.0</TargetFrameworks>
    <InformationalVersion>1.0.0+fixture</InformationalVersion>
    <IncludeSourceRevisionInInformationalVersion>false</IncludeSourceRevisionInInformationalVersion>
    <ContinuousIntegrationBuild>false</ContinuousIntegrationBuild>
    <!-- This fixture deliberately declares a type named System.Net.Http.HttpClient that collides
         (by simple and qualified name) with the framework type but lives in a different assembly and
         public key. The collision warning is the point of the fixture, not a defect. -->
    <NoWarn>$(NoWarn);CS0436;CA1822;CS1591</NoWarn>
  </PropertyGroup>
</Project>
